# DocGuardian AI — Backend dependencies
# Core (needed for the local ingestion + processing pipeline)
pydantic>=2.6
python-dotenv>=1.0

# API layer
fastapi>=0.110
uvicorn[standard]>=0.29

# Storage (Postgres + pgvector) — used once the DB is wired up
psycopg[binary]>=3.1
pgvector>=0.2.5

# Embeddings
# Local default: fastembed (ONNX Runtime, no PyTorch). Swap to Azure via env.
fastembed>=0.3
# Azure OpenAI provider (only used when EMBEDDING_PROVIDER=azure)
openai>=1.30

# Agents (LangGraph Curator/Guardian + Azure OpenAI chat)
langgraph>=0.2
langchain-openai>=0.1
langchain-core>=0.3

# MCP server — exposes DocGuardian's docs + Curator agent to MCP clients
# (e.g. GitHub Copilot CLI). Talks to the running API over HTTP (httpx).
mcp>=1.2
httpx>=0.27

# Web ingestion — crawl a URL + sub-pages and import them
beautifulsoup4>=4.12
markdownify>=0.11

# Dev / test
pytest>=8.0
